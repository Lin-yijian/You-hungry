<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.yh.mapper.AiConversationMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into ai_conversation (employee_id, title, model, message_count, create_time, update_time)
        values (#{employeeId}, #{title}, #{model}, #{messageCount}, #{createTime}, #{updateTime})
    </insert>

    <update id="update">
        update ai_conversation
        <set>
            <if test="title != null">title = #{title},</if>
            <if test="messageCount != null">message_count = #{messageCount},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </set>
        where id = #{id}
    </update>

    <select id="getById" resultType="com.yh.entity.AiConversation">
        select * from ai_conversation where id = #{id}
    </select>

    <select id="pageQuery" resultType="com.yh.entity.AiConversation">
        select * from ai_conversation
        <where>
            <if test="employeeId != null">and employee_id = #{employeeId}</if>
        </where>
        order by update_time desc
    </select>

    <delete id="deleteById">
        delete from ai_conversation where id = #{id}
    </delete>

</mapper>
